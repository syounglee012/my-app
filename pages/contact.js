import styled from "styled-components";

export default function Contact() {
  return <FooterContainer>contact me</FooterContainer>;
}

const FooterContainer = styled.div`
  display: flex;
  justify-content: center;
  bottom: 0;
  font-family: "Bodoni Moda", serif;
`;
